{
  "name": "reaction-sync-v2",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "npm run build:wasm && vite build",
    "build:wasm": "cd src/lib/sync-engine-wasm && wasm-pack build --target web --out-dir ../../../src/lib/sync-engine-wasm/pkg",
    "build:wasm:local": "cd src/lib/sync-engine-wasm && PATH=\"/home/fahad/.cargo/bin:/usr/bin:/bin:$PATH\" CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER=/usr/bin/cc /home/fahad/.cargo/bin/wasm-pack build --target web --out-dir ../../../src/lib/sync-engine-wasm/pkg",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.1",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "svelte-check": "^3.6.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@types/node": "^20.10.0"
  }
}

